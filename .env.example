# OpenAI (for /labs sales chatbot)
OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxxxxxxxxxxxxx

# SMTP (used by /api/contact and /api/labs/submit)
SMTP_USER=jose@avoqado.io
SMTP_PASS=your-smtp-password
SMTP_HOST=smtpout.secureserver.net
SMTP_PORT=587

# Labs configuration
LABS_NOTIFY_EMAIL=jose@avoqado.io

# Avoqado Tour (/demo) — demo dashboard the final CTA opens (J1 handoff).
# PUBLIC_ prefix = exposed to the client by Astro/Vite. Optional; this is the default.
PUBLIC_DEMO_DASHBOARD_URL=https://demo.dashboard.avoqado.io
