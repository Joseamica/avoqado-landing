---
import HeroCarousel from '../interactive/HeroCarousel';
import RotatingText from '../interactive/RotatingText';
import slide1 from '../../assets/hero/principal/slide1.png';
import slide2 from '../../assets/hero/principal/slide2.png';

const slides = [slide1, slide2];
---

<section class="relative min-h-screen flex items-center pt-16">
	<!-- Background decoration -->
	<div class="absolute top-1/4 right-0 w-96 h-96 bg-[--color-avoqado-green] rounded-full opacity-20 blur-3xl"></div>

	<div class="max-w-[1600px] mx-auto px-4 sm:px-6 lg:px-8 py-20">
		<div class="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center">
			<!-- Left Column - Content -->
			<div class="space-y-8">
				<h1 class="text-5xl md:text-3xl lg:text-5xl font-semibold leading-tight">
					Con avoqado, divide la cuenta, enruta tus pagos y multiplica tu <RotatingText client:load />
				</h1>

				<p class="text-xl text-gray-400 max-w-lg">
					Soluciones de pago diseñadas para todo tipo de comercio. Simplifica operaciones, mejora la experiencia del cliente.
				</p>

				<div class="flex flex-col sm:flex-row gap-4">
					<a
						href="#get-started"
						class="inline-block px-8 py-4 bg-[--color-avoqado-green] text-white text-lg font-semibold rounded-full hover:opacity-90 transition-opacity text-center"
					>
						PAGAR LA CUENTA
					</a>
					<a
						href="#demo"
						class="inline-block px-8 py-4 border-2 border-[--color-avoqado-green] text-[--color-avoqado-green] text-lg font-semibold rounded-full hover:bg-[--color-avoqado-green] hover:text-white transition-all text-center"
					>
						Ver Demo
					</a>
				</div>

				<!-- Features Quick List -->
				<div class="grid grid-cols-3 gap-8 pt-8 border-t border-gray-800">
					<div class="text-center">
						<div class="text-4xl font-bold text-[--color-avoqado-green]">Q</div>
						<p class="text-sm text-gray-400 mt-2">Escanea y Ve</p>
					</div>
					<div class="text-center">
						<div class="text-4xl font-bold text-[--color-avoqado-green]">$</div>
						<p class="text-sm text-gray-400 mt-2">Paga y Propina</p>
					</div>
					<div class="text-center">
						<div class="text-4xl font-bold text-[--color-avoqado-green]">📱</div>
						<p class="text-sm text-gray-400 mt-2">Recibo y Reseña</p>
					</div>
				</div>
			</div>

			<!-- Right Column - Visual -->
			<div class="relative">
				<HeroCarousel slides={slides} client:load />

				<!-- Background decoration -->
				<div class="absolute -bottom-8 -left-8 w-64 h-64 bg-[--color-avoqado-orange] rounded-full opacity-10 blur-2xl"></div>
			</div>
		</div>
	</div>
</section>
