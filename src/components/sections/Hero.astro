---
import HeroCarousel from '../interactive/HeroCarousel';
import RotatingText from '../interactive/RotatingText';
import Button from '../ui/Button.astro';
import slide1 from '../../assets/hero/principal/slide1.png';
import slide2 from '../../assets/hero/principal/slide2.png';

// Convert Astro image metadata to plain objects for React component
const slides = [
	{ src: slide1.src, width: slide1.width, height: slide1.height },
	{ src: slide2.src, width: slide2.width, height: slide2.height },
];
---

<section class="relative min-h-screen flex items-center pt-16" id="home">
	<!-- Background decoration -->
	<div class="absolute top-1/4 right-0 w-96 h-96 bg-avoqado-green rounded-full opacity-20 blur-3xl" aria-hidden="true"></div>

	<div class="max-w-[1600px] mx-auto px-4 sm:px-6 lg:px-8 py-20">
		<div class="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center">
			<!-- Left Column - Content -->
			<div class="space-y-8">
				<h1 class="text-4xl md:text-5xl lg:text-6xl font-bold leading-tight">
					Con avoqado, divide la cuenta, enruta tus pagos y multiplica tu <RotatingText client:load />
				</h1>

				<p class="text-lg md:text-xl text-gray-300 max-w-lg">
					Soluciones de pago diseñadas para todo tipo de comercio. Simplifica operaciones, mejora la experiencia del cliente.
				</p>

				<div class="flex flex-col sm:flex-row gap-4">
					<Button href="/#contact" variant="primary">
						Obtener demo gratis
					</Button>
					<Button href="/#features" variant="outline">
						Ver características
					</Button>
				</div>

				<!-- Features Quick List -->
				<div class="grid grid-cols-3 gap-6 md:gap-8 pt-8 border-t border-gray-800">
					<div class="text-center">
						<div class="text-3xl md:text-4xl font-bold text-avoqado-green">Q</div>
						<p class="text-xs md:text-sm text-gray-300 mt-2">Escanea y Ve</p>
					</div>
					<div class="text-center">
						<div class="text-3xl md:text-4xl font-bold text-avoqado-green">$</div>
						<p class="text-xs md:text-sm text-gray-300 mt-2">Paga y Propina</p>
					</div>
					<div class="text-center">
						<div class="text-3xl md:text-4xl font-bold text-avoqado-green">📱</div>
						<p class="text-xs md:text-sm text-gray-300 mt-2">Recibo y Reseña</p>
					</div>
				</div>
			</div>

			<!-- Right Column - Visual -->
			<div class="relative">
				<HeroCarousel slides={slides} priority={true} client:visible />

				<!-- Background decoration -->
				<div class="absolute -bottom-8 -left-8 w-64 h-64 bg-avoqado-orange rounded-full opacity-10 blur-2xl" aria-hidden="true"></div>
			</div>
		</div>
	</div>
</section>
